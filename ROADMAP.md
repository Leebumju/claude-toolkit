# 만들 스킬 목록

우선순위와 근거를 남긴다. **JD 근거**는 iOS 채용공고 원문에서 확인한 것이고,
근거가 없는 항목은 "없음" 이라고 적는다. 실전 경험만 근거인 것과 구분한다.

근거로 삼은 것은 **국내 iOS 채용공고 7건**이다 (2026-08 확인).
어디인지는 적지 않는다 — 남의 공고 문장을 옮기는 것이고, 필요한 것은 **패턴과 건수**다.

> **표본 한계.** 큰 회사 중 일부는 iOS 공고가 없었고, 일부는 채용 목록이 SPA 라
> 본문을 읽지 못했다. 원문으로 읽은 것은 7건이다.

---

## 관통하는 발견 — AI 워크플로우가 자격요건이다

우대사항이 아니라 **자격요건**에 들어가 있다. 7건 중 **3건**이 그렇다.

| 무엇을 요구하나 | 건수 |
|---|---|
| AI 도구를 개발 워크플로우의 중심에 두고 일해본 경험 (도구 이름까지 명시) | 1 |
| AI 를 활용해 생산성을 높이는 능력 (자격요건) | 1 |
| 기술스택 목록에 AI 보조 개발·자동화 도구 | 1 |

이 저장소 자체가 그 요건에 대한 답이다. 스킬을 늘리는 것과 별개로,
**절차를 도구로 만들어 쓴다는 사실**이 요구되는 항목이다.

---

## 판정 기준 — 공용인가

이 저장소는 **공개 저장소**다. 넣기 전에 이것부터 묻는다.

> **내 프로젝트 사정을 모르는 사람이 이 절차를 그대로 쓸 수 있는가.**

못 쓰면 여기 두지 않는다. 필요는 진짜여도 **자리가 여기가 아니다** —
사내 저장소의 `.claude/skills/` 에 둔다.

걸러내는 신호:

- 특정 외부 서비스에 의존한다 (번역 관리 도구, 특정 CI, 사내 서버)
- 특정 규모에서만 성립한다 (언어 20개 이상, 모듈 10개 이상)
- 우리 앱에만 있는 도메인이 절차에 들어간다

### 두 번째 기준 — Xcode 가 기본으로 주는가

WWDC26 에서 Xcode 27 이 **커스텀 스킬**(마크다운)과 **기본 제공 전문 영역**을 넣었다.
공개된 기본 영역에 **접근성 · universal sizing · 테스팅/퍼포먼스**가 들어 있다.

> **Xcode 가 기본으로 주는 것을 또 만들지 않는다.** 두 벌이 되고, 어긋난다.

**단, 이것은 아직 확인하지 못했다.** 우리 머신은 Xcode 26.4 이고 기본 제공 스킬이
실제로 무엇을 어디까지 하는지 보지 못했다. 그래서 **"확인 필요"** 로 표시하고,
Xcode 27 을 실제로 써 본 뒤에 확정한다.

반대로 **Xcode 가 줄 수 없는 것**이 우리가 만들 자리다.

| Xcode 가 줄 수 없는 것 | 왜 |
|---|---|
| 저장소별 관례에 의존하는 절차 | `legacy-port` 의 "이 저장소의 신규 패턴을 찾는다" |
| 우리가 겪은 실패 사례 사전 | `crash-triage` 의 패턴 7종 |
| 도구 자체를 검사하는 메타 도구 | `skill-audit` — Xcode 기본 스킬도 검사 대상이다 |
| 채용 과제 전형 | `hiring-assignment` |

## 상태

| 상태 | 뜻 |
|---|---|
| ✅ | 만들었고 점검 통과 |
| 🔜 | 다음 차례 |
| ⏸ | 보류 — 조건이 붙어 있다 |
| ✂ | 여기 두지 않는다 — 공용이 아니다 |

---

## A. 구조·마이그레이션

| 스킬 | 하는 일 | JD 근거 | 회사앱 | 과제 | 상태 |
|---|---|---|---|---|---|
| `legacy-port` | 레거시 화면 1개를 신규 패턴으로 옮기고 **동작 보존을 검증** (분기·순서·타이밍·에러·localized·cancel) | **주요업무 1~2번에 명시 2건** | 매일 하는 일 | 기존 코드 개선 과제 | ✅ |
| `modularize-ios` | 단일 타깃 → Tuist 모듈. 공유물 식별 → 아래로 내리기 → **위반 import 로 경계 검증** | **7건 중 5건** | 로드맵에 있고 미착수 | 실측 21분 | ✅ |
| `async-migration` | 콜백/Rx → Swift Concurrency. **취소가 실제로 전파되는지 확인하는 것까지**가 절차다 | 3건 | 명시된 장기 과제 | 간접 | ✅ |
| `di-introduce` | 싱글턴 → 주입. **규모를 먼저 재고 규모별로 전략이 갈린다** — 큰 저장소는 전부 바꾸지 않는다 | 우대 1건(DI 설계), Clean Architecture 3건 | 레거시 전반 | 테스트 가능성 | ✅ |

**결정 — `async-migration` 은 `legacy-port` 와 따로 둔다.** 특수 사례이긴 하지만
함정 목록이 다르다 (취소 전파, continuation 재개 누락, 브릿지와 네이티브 혼재).
합치면 어느 쪽 함정인지 흐려진다.

**판정 — `di-introduce` 를 만든다** (2026-08-19). `test-seams` 3-c 는 **테스트를 위한 주입**만
다룬다. 남는 것이 셋 있었다 — 모듈 경계를 넘기 위한 주입, 런타임 구현 교체,
그리고 **전역 싱글턴을 여러 개 걷어내는 이주**.

마지막이 결정적이었다. 실제 레거시 저장소를 재 보니 **싱글턴 참조 2,006건 / 471개 파일**,
그중 하나가 **1,284건 / 317개 파일**이었다. 이 규모는 리팩토링이 아니라 이주 사업이고,
`test-seams` 의 "프로토콜을 꺼내 생성자로 받는다" 로는 다룰 수 없다.

그래서 스킬의 1단계가 **세는 것**이고, 2단계는 **"전부 바꾸지 않기로 결정하는 것"** 일 수 있다.
1,000건이 넘으면 전환하지 않고 어댑터로 경계만 만든다 — 그것도 정당한 결론으로 적었다.

## B. 검증

| 스킬 | 하는 일 | JD 근거 | 회사앱 | 과제 | 상태 |
|---|---|---|---|---|---|
| `test-seams` | 테스트가 **닿을 자리**를 찾아 만든다. 붙인 테스트를 뒤집어서 실제로 지키는지 확인한다 | 자격요건에 `Testable` 1건, Swift Testing 과 XCTest 를 **병기**한 곳 2건 | 테스트가 절반뿐 | **과제 최대 실패 지점** | ✅ |
| `behavior-diff` | 리팩토링 전후 동작 동일성을 기계로 확인 (로그 대조·스냅샷) | 마이그레이션 명시 2건의 전제 | 리팩토링마다 | 드묾 | ⏸ |
| `crash-triage` | 크래시 원인 좁히기. **신호로 원인군을 가르고 고친 것을 테스트로 박제한다** | 없음 (실전 근거) | 반복 발생 | 과제에서 2건 발견 | ✅ |
| `test-audit` | **이미 있는 테스트가 실제로 지키는지** 검사. 프로덕션을 깨서 빨개지는지 본다 | 자격요건에 `Testable` 1건 | 커버리지 있으나 신뢰도 미검증 | 과제에서 초록인 죽은 테스트 10개 | ✅ |
| `skill-audit` | 만든 스킬에 무엇이 빠졌는지 점검. 읽기 전용 | 없음 (도구 위생) | — | — | ✅ |

**보류 — `behavior-diff` 는 판정을 미룬다** (2026-08-19). 겹치는 것은 확인했다 —
`legacy-port` 5단계가 이미 7항목을 대조한다. 결정적 차이는 **그것이 전부 사람이 눈으로
읽는 것**이라는 점이다 ("조건식을 나란히 놓고 읽는다", "화면에 뜨는 문자열을 비교한다").

`behavior-diff` 는 그걸 기계로 하자는 것이고, **눈으로 안 되는 규모가 실제로 오는지**를
확인해야 판정할 수 있다. 그건 `legacy-port` 를 실제로 돌려 봐야 안다.

**꺼내는 조건**: `legacy-port` 5단계 대조에서 항목이 20개를 넘거나,
같은 화면을 두 번 이상 대조하게 되면 그때 뗀다.

## C. 릴리즈·자동화

| 스킬 | 하는 일 | JD 근거 | 회사앱 | 과제 | 상태 |
|---|---|---|---|---|---|
| ~~`release-check`~~ | `assignment-submit` 의 범용화 | `CI/CD` 명시 3건 | 릴리즈마다 | 이미 있음 | ✂ |
| `ci-lane` | Fastlane / GitHub Action 레인 구성·점검 | `Fastlane`·`Github Action` 명시 2건 | CI 전환 진행 중 | 거의 없음 | ⏸ |

**`release-check` 는 만들지 않는다** (판정 2026-08-19). `assignment-submit` 의 검사를
항목별로 세어 보니 **과제 전용은 zip 규격 하나뿐**이고 나머지 다섯은 전부 범용이었다.
복제하면 두 벌이 어긋난다.

대신 `check.sh` 가 저장소 경로를 인자로 받으므로 **어느 프로젝트에서나 그대로 돌아간다.**
README 에 릴리즈·PR 용도를 명시하는 것으로 끝냈다. 스킬을 늘리지 않았다.

**`ci-lane` 보류 이유** — CI 전환이 끝난 뒤에 만들어야 실제 절차가 나온다.

## D. 표현 계층

| 스킬 | 하는 일 | JD 근거 | 회사앱 | 과제 | 상태 |
|---|---|---|---|---|---|
| ~~`i18n-audit`~~ | 다국어·RTL 점검 | 글로벌 서비스·확장 명시 2건 | 다국어 + RTL | 조사 오류 발견 | ✂ |
| `a11y-audit` | 접근성. 대비·탭 영역·VoiceOver·글자 크기 | **없음** | 미점검 | 과제에서 3건 발견 | ⏸ |
| `design-tokens` | 하드코딩 값 → 디자인 시스템 토큰 | 디자인 시스템 명시 1건 | 토큰 있으나 혼재 | 시안 정확도 | ⏸ |

**`i18n-audit` 은 이 저장소에 두지 않는다.** 절차의 핵심(번역 산출물 파일을 손으로 고치지
않는다)이 특정 번역 관리 도구를 전제하고, 언어 20개 이상·RTL 운영이라는 규모에서만 성립한다.
필요는 진짜지만 **사내 저장소의 `.claude/skills/`** 가 자리다.

다만 그 안에서 **공용인 조각 셋**은 다른 스킬에 흡수한다.

| 조각 | 어디로 |
|---|---|
| `.localized` 를 `static let` 에 캐시하면 첫 평가 시점 언어가 영구히 남는다 | `legacy-port` 4단계 (이미 있다) |
| `.left`/`.right` 대신 `leading`/`trailing` | `a11y-audit` 을 만들면 거기로 |
| `String(format:)` 문장 조립은 어순이 다른 언어에서 깨진다 | 같음 |

**`a11y-audit` 은 만들지 않는 쪽으로 기울었다.** 이유가 둘이다.

1. **JD 근거가 0이다.** 7건 중 한 줄도 없었다
2. **Xcode 27 기본 제공 전문 영역에 접근성이 들어 있다** (확인 필요)

실전 근거는 강하다 — 대비 2.01:1, VoiceOver 미독, 조사 오류를 실제로 잡았다.
그래서 버리지는 않고 **Xcode 27 기본 스킬이 그 셋을 잡는지 확인한 뒤 판단한다.**
잡으면 만들지 않고, 못 잡으면 그 구멍만 채우는 좁은 스킬로 만든다.

## E. 특수 — 해당 여부를 먼저 확인해야 한다

| 스킬 | JD 근거 | 판단 | 상태 |
|---|---|---|---|
| `webview-bridge` | Web-Native 인터페이스 명시 2건 | 대상 앱에 해당 있으면 가치, 없으면 만들지 않는다 | ⏸ |
| `push-deeplink` | 푸시·딥링크 명시 1건 | 푸시 작업 이력이 있으면 절차가 나온다 | ⏸ |

## 만들지 않는 것

- **TCA·RIBs 설명서** — 지식은 문서지 스킬이 아니다
- **아키텍처 선택 조언** — 판단이고 절차가 없다
- **면접 준비·이력서** — 반복 절차가 아니다

---

## 다음 순서

겹침 판정 셋은 끝났다 (2026-08-19) — `di-introduce` 만들었고, `release-check` 는
만들지 않기로 했고, `behavior-diff` 는 조건을 붙여 보류했다.

```
1. 스킬을 계속 실제 작업에 태운다
2. Xcode 27 확인 (막혀 있다 — 위 절 참조)
```

### 실전에 태운 기록

**스킬 여섯 개를 실제 저장소에 태워 결함 31건을 찾았다. 문서 검사로는 전부 통과했던 것들이다.**

| 스킬 | 태운 대상 | 결함 |
|---|---|---|
| `test-seams` | 레거시+신규 혼재 대형 저장소 | **11** |
| `di-introduce` | 같음 | **9** |
| `async-migration` | 같음 | **7** |
| `test-audit` | 같음 (XCTest 51파일) | **7** |
| `legacy-port` | 레거시 팝업 하나 | **4** |
| `modularize-ios` | 단일 타깃 | **1** (1단계가 통째로 무용) |
| `crash-triage` | 실기기 크래시 로그 | 절차에 없던 것 셋 (`.ips` 파싱 · `bug_type` · JetsamEvent) |
| `skill-audit` | 자기 자신 + 스킬 12개 | 검사 자체의 결함 4건이 먼저 나왔다 |

### 반복해서 나온 유형 셋

**1. 있는 것은 안 잡고 없는 것을 잡는 정규식**

| 스킬 | 잡던 것 | 실제 |
|---|---|---|
| `test-seams` | `Client` suffix | **타입 0개** (죽은 항목). 실제는 `Service` 313·`Wireframe` 257 |
| `test-audit` | `XCTAssertTrue(x.count > 0)` | **0건.** 실제는 `XCTAssertFalse(x.isEmpty)` 29건 |
| `async-migration` | continuation 3변종 | 그 저장소가 쓰는 **네 번째 변종**을 놓침 |

**패턴을 손으로 나열하면 그 저장소에 없는 것을 세고 있는 것을 빠뜨린다.**
정규식으로 묶거나, **먼저 저장소에서 실재를 세고 나서** 패턴에 넣는다.

**2. `0` 을 "깨끗함" 으로 읽는 것**

| 상황 | 표면 | 실제 |
|---|---|---|
| 전역 접근 (래퍼가 있는 저장소) | 직접 호출 71건 | 래퍼 참조 **1,254건** |
| 프로토콜 익스텐션 세탁 | `.shared` **1건** | **72파일 33건** (72배) |
| 프레임워크 판정 | `import Testing` 40건 | **전부 서드파티 체크아웃** |

**0 에 가까운 결과는 "없다" 가 아니라 "다른 이름으로 있다" 일 수 있다.**

**3. 참조 수를 도달 범위로 읽는 것**

기반 프로토콜 확장에 있던 메서드 하나. 참조 **5건**, 채택 타입 **249개**.

### 가장 위험했던 결함 — 거짓 합격

`di-introduce` 4단계가 `xargs ... 2>/dev/null` 을 썼다. iOS 저장소는 디렉토리 이름에
공백이 흔해서 `xargs` 가 거기서 쪼개져 전부 실패하는데, **`2>/dev/null` 이 그 실패를
삼켜서 "0건" 합격이 나왔다.** 아무것도 검사하지 않았는데 통과다.

**거짓 합격이 검사 없음보다 나쁘다.** stderr 를 죽이는 것을 절차에서 금지했다.

### 검증 방식 — 에이전트 보고를 그대로 받지 않았다

렌즈를 나눠 읽기 전용 에이전트에 넘기고, **결정적인 주장은 전부 직접 재확인했다.**
그 과정에서 **내가 열 개 스킬에 거꾸로 적은 설명**을 찾았다 —
`git check-ignore` 를 디렉토리로 물으면 "안전해 보인다" 고 썼는데 실제는 반대였다
(rc=1, 무시 안 됨으로 보인다). **결론은 맞고 이유가 틀렸다.**

그리고 지적 중 **하나는 거의 오탐**이었다. "두 스킬의 4단계가 절차 전체 복제" 라고 했지만
공통은 사례 하나였고 두 절의 목적이 달랐다. **재확인 없이 받으면 멀쩡한 절을 잘라냈을 것이다.**

**`modularize-ios` 1단계는 전제가 틀렸다.** `import` 로 의존 그래프를 재라고 했는데,
단일 타깃에는 내부 `import` 가 없다 — 실측하니 1,038줄이 전부 서드파티였다.
그런데 이 스킬이 쓰이는 경우가 대개 단일 타깃이다.

타입 참조로 재는 방법으로 갈랐고, **정확도 검증 절차까지 넣었다** (이름 겹침 0개,
위험 단어 하나 제외 시 96 → 95). 그 방법으로 재니 **신규↔레거시 순환이 양방향으로
실재**했고, 순환을 만드는 것이 모델·공용 뷰라는 것까지 나왔다.

**실전에 태우지 않으면 이런 것이 안 나온다.** 문서 검사로는 1단계가 통과했었다.

**1번이 남은 것 중 가장 크다.** 스킬 12개 중 실제로 태워 본 것은 `crash-triage`(실기기
로그 회수)와 `skill-audit`(자기 자신)뿐이다. 나머지는 절차가 통하는지 미검증이다.

**`test-seams` 를 둘로 쪼갰다** (2026-08-19). 4·5단계가 3단계 산출물을 참조하는 것이
0건이라 독립 스킬로 성립했고, "기존 테스트가 쓸모 있는지만 보고 싶다" 가 실제로 흔한데
지금까지는 프로덕션 변경 승인 게이트를 통과해야 거기 닿을 수 있었다.

**겹침 판정을 먼저 하는 이유** — 목록에 남은 것 중 셋이 이미 만든 스킬과 겹칠 수 있다.
겹치는 것을 따로 만들면 두 벌이 어긋난다. 만들기 전에 흡수 여부를 정한다.

`test-seams` 를 먼저 한 이유는 `legacy-port`·`modularize-ios`·`measure-ios` 세 곳이
이미 "테스트 자리가 없으면 여기로" 라고 가리키고 있었기 때문이다.
가리키는 대상이 없으면 그 줄이 폴백으로만 동작한다. 지금은 셋 다 실재하는 대상을 가리킨다.

## Xcode 27 확인 — 지금은 막혀 있다

**정식 릴리즈를 기다린다** (결정 2026-08-19). 이 머신에는 **Xcode 26.2 · 26.4** 만 있고,
`Xcode.app` 안에 스킬 관련 파일이 없고 `xcodebuild -help` 에 에이전트·스킬 옵션도 0건이다.

`xcodes list` 로 확인하니 **27.0 은 Beta 5 까지만 있고 정식이 없다** (26.x 정식은 26.6).
베타로도 확인은 되지만 **정식에서 스킬 형식이 바뀔 수 있어** 확정 근거로 삼기 어렵고,
릴리즈 작업 중에 베타를 넣을 이유가 약하다. **미뤄도 손해가 없다** — 걸려 있는 판정 셋의
기본값이 전부 "유지" 이기 때문이다.

### 베타를 넣게 되면 — 열지 않는 것까지만

위험은 하나다. **Xcode 베타는 `CoreSimulator` 를 시스템 공용으로 갱신한다.**
그러면 기존 Xcode 도 새 버전을 쓰게 된다.

```
xcodes install       → .app 만 풀린다              위험 0
.app 안을 find 로 읽기 → 스킬이 파일이면 여기서 답    위험 0
Xcode 27 을 실행      → 추가 컴포넌트 설치           CoreSimulator 갱신
```

**설치하고 열지 않으면 시스템에 손대지 않는다.** 파일에 없을 때만 실행을 다시 판단한다.
`xcodes` 는 `Xcode-27.0.0-Beta.5.app` 처럼 별 이름으로 깔아서 기존 `Xcode.app` 을 덮지 않는다.

설치 전 기준선 (2026-08-19 실측) — 설치 후 이 셋을 다시 재서 대조한다.

```
CoreSimulator   1051.49
시뮬 런타임      iOS 26.4 (23E244) · iOS 26.2 (23C54)   총 2개 15.7G
xcode-select    /Applications/Xcode.app  (Xcode 26.4, 17E192)
```

그래서 아래 판정이 **전부 보류 상태**다.

| 걸려 있는 판정 | 무엇에 달렸나 |
|---|---|
| `a11y-audit` 을 만들까 | 기본 제공 "접근성" 이 대비·VoiceOver·Dynamic Type 을 잡는가 |
| `measure-ios` 를 유지할까 | 기본 제공 "퍼포먼스" 가 어디까지 하는가 |
| `test-audit` 을 유지할까 | 기본 제공 "테스팅" 이 뒤집어 보기를 하는가 |

### Xcode 27 을 깔면 이것부터 확인한다

**막연히 "겹치는지 본다" 가 아니라 질문을 미리 정해 둔다.** 안 그러면 깔아 놓고도 안 본다.

1. **기본 제공 스킬이 파일로 있나, 아니면 서버에 있나**
   ```sh
   find /Applications/Xcode.app -iname '*skill*' -maxdepth 8
   ```
   파일로 있으면 내용을 읽어 겹침을 판정할 수 있다. 없으면 실제로 돌려 봐야 안다.

2. **커스텀 스킬을 어디에 두나** — 마크다운으로 쓴다고 했다.
   프로젝트 안인지 사용자 홈인지에 따라 이 저장소의 스킬을 **그대로 재사용할 수 있는지**가 갈린다.
   형식이 같으면 `claude-toolkit` 을 Xcode 에서도 쓸 수 있다.

3. **접근성 스킬이 무엇을 잡나** — 우리가 실전에서 잡은 셋을 던져 본다.
   대비 2.01:1 · VoiceOver 미독 · 조사(을/를) 오류.
   **셋 다 잡으면 `a11y-audit` 을 만들지 않는다.** 못 잡는 것만 좁은 스킬로 만든다

4. **퍼포먼스 스킬이 계측을 삽입하나, 읽기만 하나**
   `measure-ios` 의 값어치는 "계측을 붙이고 전후를 잰다" 에 있다.
   기본 제공이 읽기만 한다면 겹치지 않는다

5. **테스팅 스킬이 "뒤집어 보기" 를 하나**
   프로덕션을 일부러 깨서 테스트가 빨개지는지 보는 것. 이것을 하면 `test-audit` 이 겹친다

### 확인하기 전에는 스킬을 지우지 않는다

겹칠 것 같다는 **짐작으로 지우면 되돌리기 어렵다.** 지금은 전부 유지하고,
확인한 뒤에 겹치는 것만 정리한다.

---

## 실기기 경로 — 반영 완료, 남은 것

`devicectl` 은 Xcode 15 부터 있었고 우리 스킬에 **통째로 빠져 있었다.**
`Device Hub` 는 그 위에 얹은 UI 다. CLI 는 지금도 쓸 수 있다.

`measure-ios` 와 `crash-triage` 에 넣었다. **실기기에서 실제로 확인한 것**:

| 명령 | 결과 |
|---|---|
| `list devices` | 연결 상태 확인 |
| `device info files --domain-type systemCrashLogs` | `.ips` 321개 나열 |
| `device info files --domain-type appDataContainer` | 컨테이너 453개 나열 |
| `device copy to` → `copy from` | 왕복, 내용 그대로 회수 |
| `device info displays` | `bounds` · `pointScale` |
| `device info lockState` | 잠금 전제 확인 |

**실행해 보지 않은 것**: `process launch` · `sendMemoryWarning` · `suspend`/`resume` ·
`orientation set` · `sysdiagnose`. 옵션 표면만 확인했고 문서에 그렇게 적었다.

### UI 자동화는 반만 된다

`devicectl` 에 **터치 입력 명령이 없다** (`tap`·`swipe`·`gesture` 0건).
그래서 실기기 UI 자동화는 **`devicectl`(상태 제어) + XCUITest(입력)** 조합이다.
시뮬레이터에서 `axe` 로 하던 것을 실기기에서 그대로 할 수 없다.

### 보류 — `device-control` 스킬을 따로 만들까

`devicectl` 이 다루는 범위가 넓어서 별 스킬 값어치가 있어 보인다.
**지금 만들지 않는다** — `measure-ios`·`crash-triage` 를 실제로 몇 번 돌려 보고
**반복되는 것이 무엇인지 본 뒤에** 판단한다. 지금 만들면 추측이다.

---

## 언젠가 쪼갤 것 — 플러그인 단위

플러그인은 **통째로만 켜진다.** 스킬을 골라 켤 수는 없다.
`ios-modernize` 가 6개가 되면서 매 세션 **~1,044 토큰**을 먹는다.

| 플러그인 | 스킬 | 매 세션 |
|---|---|---|
| `ios-modernize` | 6 | ~1,044 |
| `hiring-assignment` | 3 + 에이전트 1 | ~625 |
| `skill-forge` | 1 | ~159 |
| `ios-measure` | 1 | ~145 |
| `code-lens` | 에이전트 1 | ~137 |

### 쪼개는 조건

지금은 쪼개지 않는다. **여섯 개가 한 작업 흐름 안에 있어서** 하나만 켜는 경우가 드물다
(레거시를 옮기다 보면 경계·테스트·비동기·크래시가 순서대로 나온다).

아래 중 하나가 오면 쪼갠다.

- `ios-modernize` 가 **9개를 넘는다**
- 매 세션 비용이 **1,500 토큰을 넘는다**
- 실제로 "이것만 켜고 싶다" 가 생긴다 — 짐작하지 않고 그때 판단한다

### 쪼갤 때의 선

성격으로 가른다. 지금 미리 정해 두면 나중에 즉흥적으로 안 나눈다.

```
ios-migrate   legacy-port · async-migration · modularize-ios   (구조를 옮긴다)
ios-verify    test-seams · test-audit · crash-triage           (지키는지 검증한다)
```

`ios-measure` 는 이미 따로 있으니 `ios-verify` 로 합칠지도 그때 본다.

---

## 스킬을 하나 만들 때마다 하는 것

```sh
./plugins/skill-forge/skills/skill-audit/audit.sh plugins
```

`✗` 가 0이어야 한다. **검사가 틀렸다고 판단되면 대상 스킬이 아니라 검사를 먼저 고친다.**
